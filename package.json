{
  "name": "contextproof",
  "version": "0.1.0",
  "private": true,
  "description": "ContextProof hackathon vertical slice.",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
